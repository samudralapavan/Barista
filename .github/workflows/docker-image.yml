name: Docker Image CI

on:
  push:
    branches: [ "pavans" ]
  pull_request:
    branches: [ "pavans" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag barsita-cafe:$(date +%s)
